# 为什么必须走 devskill（先读这段）

本仓是多机、多会话、多模型并行协作的单工作树。**真值只在 git**：没有需求文档/plan/worklog 的代码，换台机器、换个会话就无法接手、无法回溯、无法评审；口头或聊天里的"做完了"一律不算数。每一次"直接写码"都会在协作链上付出数倍返工代价：进度对不上账、并行改动互相冲掉、同一问题重复排查。写文档的几分钟，买的是整条协作链的可信。

流程规范全文在 **`devskill/`**（以 `SKILL.md` 为入口；`DISCIPLINE.md` / `WORKLOG-PROTOCOL.md` / `templates/` 随规范逐字拷贝使用，禁止现场改写）。此处不展开，照做即可。

## 接入你的项目

1. 把整个 `devskill/` 目录放在工作区最顶层（仓库根目录，与本文件并列）。
2. 把本文件拷为项目根目录的 `AGENT.md`（或并入已有的 `AGENTS.md`），把尖括号占位替换成本项目实际内容，并删除本「接入」一节。
3. 在 `docs/` 下建 `REGISTRY.md`（板块登记表：每行 = 板块路径 + 干什么，≤100 字）；之后每个新板块在同一 commit 登记一行，漏登 = 违规。
4. 部署/发版等其他流程若有，同样沉淀成文档，并在本文件登记入口。

# 什么情况，对应做什么

**新功能**（不管改动多小，哪怕只改一行代码）：
必须按 devskill 模板走全流程：新版目录逐字拷模板 → 五段需求文档（现象/目标/方案与取舍/不做什么/验收口径）→ plan.md（P0..PN，逐项可判定验收）→ **用户确认计划** → 写码 → 做一个勾一个 + worklog → commit 带板块条目号（如 `<板块名> v2 P9-T24`）→ push。不 push 等于没做。

**修复 / 查 bug**：
1. **先溯源**：在 `docs/REGISTRY.md` 找到该功能当初的开发板块；查 bug 必须优先参考所有 devskill 派生文档（需求/设计/BOUNDARY/review），**找到根本原因再决定动作**——禁止不读文档就改码。
2. **再定板块动手**：在根因所属板块最新版完成全链记录——现象/根因补进文档 → 补 plan 条目 → 修复 → 补 worklog（changelog）→ commit 带条目号 → push。
3. **没有对应分类**：必须从 devskill 派生新类别（按新功能流程建板块），禁止无处安放就直接写码。

**答不出归属**：立即停工，让用户指定 docs 路径；禁止直接写码。

**例外（无需板块条目）**：纯 docs/worklog 改动；当日拆除的临时排障探针（当日必清，遗留即违规）。

# 项目硬约束

> 以下为示例，接入时替换为本项目自己的红线；第二条是 devskill 通用红线，保留。

- **`<第三方源码目录>`：只读**。vendored 上游源码（含上游内核）的改动会在下次同步时被覆盖或制造冲突，且破坏与上游对拍的测试基线。允许读/搜索/装依赖/构建/跑测试；禁止新增、修改、删除、移动、重命名任何源代码文件。某操作会连带改动源代码 → 跳过并向用户说明。
- **`docs/` 下需求文档、worklog、review/、BOUNDARY.md：禁止删除**（旧版 plan 稳定后的删除须用户确认）。版本目录之外禁止散落文档。

# 接手协议

`git pull` → 本文件 → `docs/REGISTRY.md` → 目标板块 `BOUNDARY.md` → 最新版需求/plan → **先复述再动手**。
